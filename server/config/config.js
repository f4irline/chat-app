const config = {};

config.mysql = {
    host: 'your-db-host',
    user: 'your-db-user',
    password: 'your-db-password',
    database: 'your-db'  
}

config.port = 3000;
config.host = 'localhost'

module.exports = config;